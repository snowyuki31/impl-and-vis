import {
  GeneratorProps,
  SolverProps,
  InfoProps,
  StateHooks,
} from "../../../types/graphColoring";

const GraphColoring = ({ hooks }: { hooks: StateHooks }) => {
  return <></>;
};

export default GraphColoring;
