import {
  GeneratorProps,
  SolverProps,
  InfoProps,
  StateHooks,
} from "../../../models/graphColoring";

const GraphColoring = ({ hooks }: { hooks: StateHooks }) => {
  return <></>;
};

export default GraphColoring;
